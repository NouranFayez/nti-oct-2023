users 
{
    id
    name
    age
    email
    addresses : [
        {addressType:"", addressDetails:""}
    ]
}

add user => inputs => name, age, email, defaults => addresses [], id: date
add address to user => address details, type => push addresses

